export interface City {
  id: string;
  name: string;
  population?: number;
  tagline?: string;
  coverImage?: string;
}

export interface Food {
  id: string;
  name: string;
  description?: string;
  image?: string;
}
